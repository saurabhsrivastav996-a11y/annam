# Leave empty to route /api and /socket.io through the Vite dev proxy (recommended for local dev).
# Set to your deployed API origin in production, e.g. https://annam-api.onrender.com
VITE_API_URL=

# Optional: only needed if you swap the Leaflet/OpenStreetMap map for Google Maps
VITE_GOOGLE_MAPS_API_KEY=
